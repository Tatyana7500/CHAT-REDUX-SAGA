const defaultState = {
    email: '',
    name: '',
    password: '',
    errorText: '',
    isOpenErrorWindow: false,
};

const signInReducer = (state, action) => {
    switch (action.type) {
        case ...;
    }

    return state;
};